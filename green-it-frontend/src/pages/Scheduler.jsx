import React from "react";

const Scheduler = () => (
  <div className="p-6">
    <h2 className="text-xl font-bold">Task Scheduler</h2>
    <p>Monitor and simulate scheduled tasks.</p>
  </div>
);

export default Scheduler;